name: 📔 Reference Docs

on: [push]

env:
  CI: true
  DOCS_REPO: "https://github.com/jacob-ebey/temp-docs-test.git"
  DOCS_FILE: "build.json"

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: ⬇️ Checkout repo
        uses: actions/checkout@v4

      - name: 🏗 Build docs
        run: |
          touch build.json
          echo '{ "ref_name": "${{ github.ref_name }}" }' > build.json

      - name: ⚙️ Configure git
        run: |
          git config --unset-all http.https://github.com/.extraheader
          git config --global user.email "github-bot@github.com"
          git config --global user.name "Docs Bot"
          git config --global http.version HTTP/1.1
          git remote add docs ${{ env.DOCS_REPO }}
          git fetch docs

      - name: ⬆️ Commit and push docs
        run: |
          cp ${{ env.DOCS_FILE }} ../api.json
          git reset --hard HEAD
          git checkout docs/main
          git checkout -b temp_docs__${{ github.ref_name }}
          mv ../api.json api.json
          git add api.json
          git commit -m "chore: update api.json"
          git push -f -u docs HEAD:${{ github.ref_name }}